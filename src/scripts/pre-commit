#!/bin/bash
# Pre-commit hook for SpeedBitCrackV3
# Ensures code quality before commits

echo "🛡️  SpeedBitCrackV3 Pre-commit Quality Gate"
echo "==========================================="

# Colors
RED='\033[0;31m'
GREEN='\033[0;32m'
NC='\033[0m'

# Run quality check
if ./scripts/quality_check.sh; then
    echo -e "${GREEN}✅ Pre-commit quality checks passed${NC}"
    exit 0
else
    echo -e "${RED}❌ Pre-commit quality checks failed${NC}"
    echo "Fix issues before committing, or use --no-verify to bypass"
    exit 1
fi