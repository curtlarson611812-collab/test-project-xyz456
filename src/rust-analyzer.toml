# Rust Analyzer Configuration for SpeedBitCrackV3
# Minimal working configuration - only well-supported options

[imports]
# Configure import handling
granularity.group = "crate"
group.enable = true
prefix = "crate"

[completion]
# Completion settings
autoimport.enable = true

[diagnostics]
# Diagnostic settings
enable = true

